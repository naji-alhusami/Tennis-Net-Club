export async function editDataInMongo(timeInfo) {
    console.log(timeInfo);
    // const response = await fetch("/api/timeSlots/editTimeSlots", {
    //   method: "POST",
    //   body: JSON.stringify(generatedTimes),
    //   headers: {
    //     "Content-Type": "application/json",
    //   },
    // });
    // const timeSlotsData = await response.json();
  
    // if (!response.ok) {
    //   throw new Error(timeSlotsData.message || "Something went wrong!");
    // }
  }
  